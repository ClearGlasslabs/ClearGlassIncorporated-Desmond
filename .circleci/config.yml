version: 2.1

executors:
  py:
    docker:
      - image: cimg/python:3.11-node
    resource_class: medium
    working_directory: ~/project

jobs:
  da:
    executor: py
    steps: [checkout, {run: "python scripts/circleci_probe.py internal classification-root-prefix-da"}]
  db:
    executor: py
    steps: [checkout, {run: "python scripts/circleci_probe.py internal classification-root-prefix-db"}]
  dc:
    executor: py
    steps: [checkout, {run: "python scripts/circleci_probe.py internal classification-root-prefix-dc"}]
  dd:
    executor: py
    steps: [checkout, {run: "python scripts/circleci_probe.py internal classification-root-prefix-dd"}]
  de:
    executor: py
    steps: [checkout, {run: "python scripts/circleci_probe.py internal classification-root-prefix-de"}]
  df:
    executor: py
    steps: [checkout, {run: "python scripts/circleci_probe.py internal classification-root-prefix-df"}]
  dg:
    executor: py
    steps: [checkout, {run: "python scripts/circleci_probe.py internal classification-root-prefix-dg"}]
  dh:
    executor: py
    steps: [checkout, {run: "python scripts/circleci_probe.py internal classification-root-prefix-dh"}]
  di:
    executor: py
    steps: [checkout, {run: "python scripts/circleci_probe.py internal classification-root-prefix-di"}]

workflows:
  exact-d-prefix:
    jobs:
      - da
      - db
      - dc
      - dd
      - de
      - df
      - dg
      - dh
      - di
